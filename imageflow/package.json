{
  "name": "imageflow",
  "version": "0.1.0",
  "private": true,
  "description": "Nextcloud app for fast and safe photo sorting workflows.",
  "scripts": {
    "check": "bash scripts/self-check.sh",
    "test:browser": "playwright test",
    "test:browser:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}

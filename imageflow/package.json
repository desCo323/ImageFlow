{
  "name": "imageflow",
  "version": "0.1.3",
  "private": true,
  "description": "Nextcloud app for fast and safe photo sorting workflows.",
  "scripts": {
    "check": "bash scripts/self-check.sh",
    "test:browser": "playwright test",
    "test:browser:auth": "IMAGEFLOW_AUTH_TESTS=1 playwright test tests/Browser/*.auth.spec.js --trace=off --reporter=line",
    "test:browser:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
